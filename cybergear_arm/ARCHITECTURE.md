# ARCHITECTURE — cybergear_arm 구조와 원리

이 문서는 "코드가 **왜** 이렇게 생겼는가"를 설명합니다. 사용법은 [README.md](README.md)를 보세요.

---

## 1. 계층 구조 한눈에 보기

```
 ┌──────────────────────────────────────────────────────────────────┐
 │  사용자 코드 (역기구학 / 궤적 생성 / C# GUI 등)                   │
 │        │  /arm/reference  (JointTrajectoryPoint)                 │
 ├────────▼─────────────────────────────────────────────────────────┤
 │  ROS2 노드  arm_controller_node.cpp                               │
 │    ├ ROS 스레드   : 구독 / 파라미터 / 로그                          │
 │    └ 제어 스레드  : 고정 주기 루프 (clock_nanosleep)                │
 ├──────────────────────────────────────────────────────────────────┤
 │  제어 계층   SmcController  ◄── DhDynamics (M, C, G, F) ◄ model.yaml │
 ├──────────────────────────────────────────────────────────────────┤
 │  다축 계층   CyberGearBus : 관절 ↔ (버스, 모터 ID), 방향·오프셋     │
 ├──────────────────────────────────────────────────────────────────┤
 │  프로토콜    cybergear_protocol : CanFrame ↔ 토크/위치/속도         │
 ├──────────────────────────────────────────────────────────────────┤
 │  전송 계층   CanTransport (인터페이스)                              │
 │     ├ SerialAtTransport  (CH340 'AT' 시리얼, 지금 사용)             │
 │     ├ SocketCanTransport (can0, can1 … — 6축 1 kHz용)              │
 │     └ SimTransport       (가짜 모터 + 동역학 적분)                  │
 └──────────────────────────────────────────────────────────────────┘
```

**설계 원칙: 아래 계층은 위 계층을 모릅니다.**

- 프로토콜 코드는 ROS를 모릅니다. 그래서 C# GUI나 다른 프로그램으로 옮기기 쉽습니다.
- 제어기는 모터가 시리얼로 연결됐는지, CAN으로 연결됐는지, 시뮬레이터인지 모릅니다.
- 그 덕분에 어댑터를 바꾸거나 시뮬레이터로 시험할 때 **전송 계층 한 줄만** 바꾸면 됩니다.

`cybergear_core` 라이브러리(ROS와 무관)와 ROS 노드를 CMake에서 분리한 이유도 같습니다.

---

## 2. 한 주기 안에서 일어나는 일

```
시간 ─────────────────────────────────────────────────────────────►
 │◄──────────── 한 주기 (500 Hz = 2 ms) ────────────►│
 ▼                                                    ▼
 wake ─ poll() ─ 안전검사 ─ SMC 계산 ─ send×N ─ (publish) ─ sleep ─ wake ...
         │                                │
         │ 지난 주기 명령에 대한 모터 응답  │ 이번 명령 → 모터가 응답 → 다음 주기 poll()에서 읽음
```

- **1주기 지연:** CyberGear는 명령을 받을 때마다 피드백을 1개 보냅니다. 하지만 USB 왕복 시간 때문에 그 응답은 보통 **다음 주기**에 도착합니다. 그래서 제어기는 1주기 전의 상태를 보고 계산합니다. 500 Hz에서 2 ms 지연이며, SMC의 λ를 너무 크게 잡으면 이 지연 때문에 떨림이 생깁니다.
- **절대 시각 수면:** `clock_nanosleep(TIMER_ABSTIME)`은 "다음 주기의 절대 시각"까지 잡니다. 계산 시간이 들쭉날쭉해도 주기 오차가 **누적되지 않습니다**.
- **publish 간격 줄이기:** 매 주기 publish하면 메모리 할당과 직렬화 부하가 생깁니다. 그래서 `publish_decimation` 주기마다 한 번만 보냅니다.

---

## 3. ROS2로 로봇을 제어하는 원리 (강의)

### 3-1. ROS2가 해 주는 일과 해 주지 않는 일

| ROS2가 잘하는 것 | ROS2가 보장하지 않는 것 |
|---|---|
| 프로그램(노드) 간 데이터 전달 (토픽) | **정확한 주기** |
| 설정 관리 (파라미터, yaml, launch) | 메시지 도착 시간 |
| 기록·재생 (`ros2 bag`), 시각화 (rqt, RViz) | 하드 실시간성 |

토픽은 DDS라는 네트워크 미들웨어 위에서 동작합니다. 같은 컴퓨터 안에서도 직렬화, 복사, 스케줄링을 거치므로 **수백 µs~수 ms의 흔들림**이 생길 수 있습니다.

### 3-2. 그래서 "안쪽 루프는 한 프로세스 안에서" 돌립니다

```
  바깥 루프 (느림, 10~200 Hz)          안쪽 루프 (빠름, 500~1000 Hz)
  ┌──────────────┐   토픽    ┌───────────────────────────────┐
  │ 역기구학/궤적  │ ───────► │ 상태읽기 → SMC → 토크 전송      │
  └──────────────┘           │ (한 스레드, 토픽 없음)          │
                             └───────────────────────────────┘
```

- 목표 궤적(`/arm/reference`)은 토픽으로 받습니다. 조금 늦게 와도 괜찮습니다. 위치·속도·가속도를 같이 받으니, 도착 사이 구간은 마지막 값이 사용됩니다.
- **상태 → 제어 → 명령**은 절대 토픽을 거치지 않고, 한 스레드 안에서 함수 호출로 처리합니다.

### 3-3. 정석: `ros2_control`

ROS2 로봇팔의 표준 구조는 `ros2_control`입니다. 이 패키지와 대응시켜 보면 다음과 같습니다.

| ros2_control | 이 패키지 |
|---|---|
| `hardware_interface::SystemInterface` (`read()` / `write()`) | `CyberGearBus::poll()` / `sendTorque()` |
| `controller_manager` (실시간 루프) | `controlLoop()` 스레드 |
| `controller` (예: `effort_controllers`) | `SmcController` |
| URDF `<ros2_control>` 태그 | `config/arm_2dof.yaml`의 관절 설정 |

지금은 **구조를 한눈에 이해하기 위해** 단일 노드로 만들었습니다. 6축으로 확장할 때 `CyberGearBus`를 감싸는 `SystemInterface` 플러그인을 만들면, MoveIt 등 ROS 생태계와 바로 연결됩니다. 코어 라이브러리를 ROS와 분리해 둔 것이 이 이전(migration)을 쉽게 해 줍니다.

### 3-4. 스레드와 데이터 공유

- ROS 스레드(`rclcpp::spin`)가 `/arm/reference`를 받아 `ref_q_` 등에 씁니다.
- 제어 스레드가 이 값을 읽습니다.
- 두 스레드가 같은 변수를 건드리므로 `std::mutex`로 보호합니다. 잠그는 구간은 벡터 3개 복사뿐이라 아주 짧습니다.
- 제어 스레드는 `SCHED_FIFO`(실시간 우선순위)로 올려서 다른 프로그램보다 먼저 실행되게 합니다.
- 더 엄격하게 하려면 PREEMPT_RT 커널과 CPU 격리(`isolcpus`)까지 적용합니다.

---

## 4. 슬라이딩 모드 제어 (SMC)

### 4-1. 식

```
e    = q − q_d
q̇_r  = q̇_d − Λe          (기준 속도)
q̈_r  = q̈_d − Λė          (기준 가속도)
s    = q̇ − q̇_r = ė + Λe   (슬라이딩 변수)

τ = M̂(q)q̈_r + Ĉ(q,q̇)q̇_r + Ĝ(q) + F̂(q̇)  − K∘sat(s/φ) − K_d∘s
    └──────── 모델 기반 (등가 제어) ────────┘  └─ 강인항 ─┘  └ 댐핑 ┘
```

### 4-2. 왜 동작하는가

리아푸노프 함수 `V = ½ sᵀM s`를 잡으면, `Ṁ − 2C`가 반대칭이라는 성질 덕분에 다음이 성립합니다.

```
V̇ = sᵀ(Δ − K∘sat(s/φ) − K_d s)
```

여기서 Δ는 "모델이 틀린 만큼의 토크"입니다. 경계층 밖에서 `K_i > |Δ_i|`이면 V̇ < 0이므로 s → 0이고, 결국 e → 0입니다.

→ **K는 곧 "모델을 얼마나 못 믿는가"의 예산입니다.** 이 로봇처럼 치수가 부정확하면 K가 커야 합니다.

> `Ṁ − 2C` 반대칭은 이 패키지의 2축 모델에 대해 직접 확인했습니다 (`arm_dynamics.hpp` 주석).

### 4-3. 채터링: 원인과 해결 (실측 영상 분석 결과)

**증상:** 실제 2축에서 SMC를 돌렸더니 팔이 계속 떨렸습니다. 영상의 소리를 주파수 분석하면 **약 45 Hz**와 그 배음(~95 Hz)이 영상 내내 나타납니다.

**원인 = 속도 피드백 + 통신 지연.**

HOST 방식에서 속도에 걸리는 실효 이득은 `K/φ + Kd`입니다. 이 피드백이 전체 지연 τ(USB·어댑터·1주기)를 겪으면, 위상이 −180°가 되는 주파수에서 발진합니다.

```
관성 J 에 속도 피드백 Kv, 지연 τ  →  발진 주파수 ≈ 1 / (4τ)
45 Hz  →  τ ≈ 5.5 ms   (CH340 USB 지연 + 500 Hz 1~2주기로 설명되는 크기)
안정 조건 ≈  Kv / J  <  π / (2τ)
```

이전 기본값 `K/φ + Kd = 0.4/0.3 + 0.2 ≈ 1.5`는 링크 2의 작은 관성(약 0.005 kg·m²)에 비해 너무 컸습니다.

**sign → sat 경계층만으로는 부족합니다.** 경계층 안에서의 SMC는 "기울기 K/φ인 선형 피드백"입니다. 그래서 불연속성이 아니라 **지연된 고이득 피드백**이 문제입니다.

**ISMC(적분 슬라이딩 모드)는 이 문제를 해결하지 못합니다.**
- ISMC는 도달 단계를 없애서 처음부터 강인성을 주는 기법입니다.
- 스위칭 항과 지연은 그대로이므로 채터링 원인이 그대로 남습니다.
- 시뮬레이터에서 적분항을 더해 보면, 추종 중에는 오히려 오차가 커졌습니다 (README 표의 D).
- 적분항은 **정지 자세의 정상상태 오차**를 없앨 때만 쓰세요.

**해결 = 선형 피드백을 모터 안으로 옮기기 (`smc.inner_loop: motor`).**

CyberGear 운동제어 모드는 모터 내부에서 다음을 계산합니다.
```
τ_motor = kp(p_ref − q) + kd(v_ref − q̇) + τ_ff
```
이 계산은 USB 지연 없이 모터 자신의 빠른 제어 주기에서 돕니다. 그런데 SMC의 선형 부분은 PD와 같은 형태입니다.
```
−(Kd + K/φ)·s = −(Kd + K/φ)·ė − Λ(Kd + K/φ)·e
             =  kd_int(q̇_d − q̇) + kp_int(q_d − q)
  kd_int = Kd + K/φ,   kp_int = Λ·kd_int
```
따라서 `p_ref = q_d`, `v_ref = q̇_d`, `kp = kp_int`, `kd = kd_int`를 보내고, PC는 느린 부분만 계산하면 됩니다.
```
τ_ff = M(q)q̈_d + C(q,q̇_d)q̇_d + G(q) + F(q̇_d) − Ki∫s
```
- 경계층 안에서는 HOST 방식과 **같은 제어법칙**입니다.
- 경계층 밖에서는 선형항이 K보다 커지므로, 도달 조건이 오히려 더 강하게 만족됩니다.
- 대신 토크가 커질 수 있어서, 모터의 `limit_torque`(0x700B)를 `torque_limits`로 설정해 둡니다.
- 모델 항에는 측정 속도 대신 **목표 속도**를 씁니다 ("desired compensation"). 지연·노이즈가 있는 측정 속도에 모델 항을 곱하지 않기 위해서입니다.

**그래도 떨리면 확인할 것:**
1. `kd_int`가 너무 큰지 봅니다. 모터 안에서도 속도 추정 노이즈가 있습니다. 시작 로그에 `모터 내부 PD: kp=… kd=…`가 출력됩니다.
2. `velocity_filter_hz`는 **켜지 마세요**. 필터는 위상 지연을 늘려 HOST 방식의 발진을 더 악화시킵니다. MOTOR 방식에서는 제어에 거의 쓰이지 않습니다.
3. 기구적 원인도 확인하세요: 볼트 풀림, 프로파일·브래킷 유격, 백래시. 이런 문제는 45 Hz 같은 고정 주파수의 "윙" 소리로 나타날 수도 있습니다.

### 4-4. 튜닝 순서 (MOTOR 방식)

1. **K = 0, Kd = 0.05, λ = 5**로 시작합니다. 중력 보상(모델)만으로 팔이 "떠 있는지" 보세요. 한쪽으로 처지면 모델의 `mass`, 무게중심이 틀린 것입니다.
2. **Kd**를 올려 모터 내부 `kd_int`가 0.3~1 정도가 되게 합니다. 손으로 밀었을 때 "끈적하게" 버티면 좋은 상태입니다.
3. **λ**를 5 → 10 → 20으로 올립니다. 모터 내부 `kp_int = λ·kd_int`가 커지므로 위치 강성이 올라갑니다.
4. **K, φ**: `K/φ`가 `kd_int`에 더해집니다. 모델 오차 예산 K를 정했다면 φ로 선형 기울기를 조절하세요.
5. **Ki**는 마지막에, 정지 자세 정밀도가 필요할 때만 씁니다.
6. `smc.use_model: false`와 비교하면 모델의 기여도를 정량화할 수 있습니다.

---

## 5. 동역학 모델 (2축)

### 5-1. 기구와 가정

```
        z0 (전역 z)
        │     x2 (q2=0 일 때 위쪽)
        │     ▲  ← 20×20 프로파일 (링크 2), 무게중심 lc2
        │     │
        │  ┌──┴──┐ 모터2 (축 = z1, 수평)
        │  └──┬──┘
        │     │ d2 (z1 방향 오프셋)
      ┌─┴─┐ ──●── o1 (높이 d1)
      │ M1│  모터1 (축 = z0)
      └───┘
```

| 요소 | 모델링 | 파라미터 |
|---|---|---|
| 모터 1 하우징 | 바닥 고정 → 무시 | — |
| 모터 2 본체 | 관절 1과 함께 도는 **원통** (축 수평) | `motor_mass`, `motor_radius`, `motor_length`, `motor2_offset` |
| 링크 2 프로파일 | **균일 사각 막대** `m = ρL`, `I = m(L²+a²)/12` | `profile_length`, `profile_lin_density`, `profile_side`, `profile_start` |
| 브래킷·끝단 | 점질량 | `bracket2_*`, `tip_*` |
| 모터 회전자 | 감속비² × 회전자 관성 (반사 관성) | `rotor_reflected1/2` (모르면 0) |
| 마찰 | 점성 + 쿨롱 (tanh 근사) | `viscous*`, `coulomb*` |

### 5-2. 결과 (s = sin q₂, c = cos q₂)

```
A = Izz1 + m2·d2² + Ia2 + Jr1        B = m2·lc2² + It2 − Ia2
D = m2·d2·lc2                         E = It2 + m2·lc2² + Jr2

M = [ A + B s²   −D c ]      C = [  B s c q̇2    B s c q̇1 + D s q̇2 ]      G = [ 0            ]
    [ −D c        E   ]          [ −B s c q̇1          0            ]          [ −m2 g lc2 s  ]
```

**검증:** DH 변환행렬로 야코비안을 수치 계산한 `Σ(m Jvᵀ Jv + Jωᵀ I Jω)` 결과와 위 M이 여러 자세에서 일치함을 확인했습니다.

**해석:**
- `M11`이 q₂에 따라 바뀝니다. 팔을 눕히면(s² ↑) 관절 1이 돌려야 하는 관성이 커집니다.
- `M12 = −D c`: 오프셋 d₂ 때문에 두 관절이 서로 영향을 줍니다. d₂ = 0이면 사라집니다.
- G는 관절 2에만 있습니다. 관절 1의 축이 수직이기 때문입니다.

### 5-3. N축 범용 모델 (`DhDynamics`) — 4축·6축은 이것으로

로봇 구조를 코드가 아닌 **yaml 파일**(`config/model_Ndof.yaml`)로 적습니다.

```yaml
joints:
  - name: joint2
    dh: {theta_offset_deg: 90, d: 0.05, a: 0.0, alpha_deg: 0}
    armature: 0.0            # 회전자 반사 관성
    link:                    # 이 관절이 움직이는 링크 (frame i 기준 좌표)
      - {type: profile,  lin_density: 0.45, side: 0.02, length: 0.20, start: [0,0,0], axis: x}
      - {type: cylinder, mass: 0.317, radius: 0.04, length: 0.042, pos: [0,0,0.03], axis: z}
      - {type: point,    mass: 0.05, pos: [0.2, 0, 0]}
```

- 부품들은 평행축 정리로 **하나의 강체**(질량, 무게중심, 관성 텐서)로 합쳐집니다.
- **다음 관절의 모터 본체는 "이전 링크"의 부품**입니다. 예: 모터 3 하우징은 링크 2에 적습니다.
- 계산 방법:
  - `M = Σ(m Jvᵀ Jv + Jωᵀ R I Rᵀ Jω)`
  - `G = −Σ m Jvᵀ g`
  - C는 Christoffel 기호로 구합니다. 이때 ∂M/∂q는 중앙 차분으로 계산합니다.
- 검증 (`model_test`):
  - 2축 해석해와 일치 (차이 ~1e-13)
  - 4축에서 `Ṁ − 2C` 반대칭
  - 4축에서 `G = ∂V/∂q`
- 계산 시간: 4축 약 9 µs (개발 PC 기준). 500 Hz 주기(2 ms)에 비해 충분히 작습니다.
- 6축에서 더 빠르게 하려면 RNEA나 Pinocchio로 교체할 수 있습니다. `ArmDynamics` 인터페이스만 맞추면 됩니다.

---

## 6. 통신 대역폭과 6축 1 kHz 계획

| 구간 | 프레임 1개 | 최대 처리량 |
|---|---|---|
| USB 시리얼 921600 bps | 17바이트 ≈ 170비트 | ≈ 5,400 프레임/s (한 방향) |
| CAN 1 Mbps | 확장 8바이트 ≈ 130~155비트 | ≈ 6,500~7,500 프레임/s (양방향 합) |

한 주기 필요량 = 모터 수 × (명령 1 + 응답 1)

| 구성 | 필요량 | CAN 버스 부하 | 판정 |
|---|---|---|---|
| 모터 2개 × 500 Hz, 버스 1개 | 2,000 프레임/s | ~28% | ✅ 여유 |
| 모터 2개 × 1 kHz, 버스 1개 | 4,000 프레임/s | ~56% | ✅ 가능 (USB 지연 주의) |
| 모터 4개 × 500 Hz, 버스 1개 | 4,000 프레임/s | ~56% | ✅ 가능 (현재 4축) |
| 모터 4개 × 1 kHz, 버스 1개 | 8,000 프레임/s | ~112% | ❌ 불가 |
| 모터 6개 × 1 kHz, 버스 1개 | 12,000 프레임/s | ~170% | ❌ 불가 |
| **모터 6개 × 1 kHz, 버스 3개 (2개씩)** | 버스당 4,000 | ~56% | ✅ **권장** |
| 모터 6개 × 500 Hz, 버스 1개 | 6,000 프레임/s | ~85% | ⚠️ 위험 |

이 코드는 이미 **여러 버스를 지원**합니다. yaml만 이렇게 바꾸면 됩니다.

```yaml
transport: "socketcan"
buses: ["can0", "can1", "can2"]
motor_ids:   [1, 2, 3, 4, 5, 6]
bus_indices: [0, 0, 1, 1, 2, 2]
```

---

## 7. 안전 상태 기계

```
 시작 ─► startAll() ─► 첫 피드백 대기(1 s) ─┬─ 실패 ─► stopAll() + 종료
                                            └─ 성공 ─► RUN (현재 자세 유지)
 RUN ── (에러코드 / 속도초과 / 관절한계 / 피드백 끊김) ──► SAFE (latch)
 SAFE : 모든 모터에 "토크 0 + 모터 내부 Kd" → 팔이 천천히 내려옴. 재시작해야 해제.
 종료 : 0.5 s 댐핑 → stopAll() (모터 풀림)
```

SAFE 모드에서 모터를 바로 풀지 않고 **댐핑**을 거는 이유: 풀어 버리면 중력 때문에 팔이 자유낙하합니다.

---

## 8. 기구학 보정 실험과의 연결

이 로봇의 오차는 두 종류이고, **서로 섞이지 않게 분리**하는 것이 실험 설계의 핵심입니다.

| 오차 | 원인 | 어디에 나타나는가 | 이 코드에서 |
|---|---|---|---|
| **관절 추종 오차** e = q − q_d | 제어 성능, 동역학 모델 오차 | 모터 엔코더 | `/arm/debug`의 e, s |
| **기구학 오차** | DH 파라미터(길이·각도) 부정확 | 외부 측정(카메라, 게이지)과 FK 결과 차이 | 사용자 보정 이론 |

- 보정 데이터를 모을 때는 **정지 자세에서** e가 충분히 작아진 뒤(예: |e| < 0.001 rad) 외부 측정값을 기록하세요. 그래야 제어 오차가 기구학 오차로 잘못 섞이지 않습니다.
- 동역학 모델의 `lc2`, `d2`도 DH와 같은 기하 정보입니다. 보정한 DH를 모델에 다시 넣으면 SMC 성능도 좋아집니다. 이 **"보정 → 모델 갱신 → 제어 개선"** 루프 자체가 좋은 실험 주제가 될 수 있습니다.

---

## 9. 알려진 한계 / TODO

- [ ] 실제 하드웨어 미검증 (시뮬레이터 + 오프라인 테스트만 통과)
- [ ] CyberGear 치수·회전자 관성 실측 필요
- [ ] 속도 피드백 노이즈에 대한 필터 튜닝 (`velocity_filter_hz`)
- [x] 시뮬레이터에 통신 지연 모델 추가 (`sim.feedback_delay`)
- [x] N축 범용 동역학 (`DhDynamics`)
- [ ] 4축 실제 DH·치수 입력
- [ ] 모터 내부 PD 이득의 실제 한계 측정 (kd_int 를 올렸을 때 모터 자체 떨림)
- [ ] `ros2_control` 하드웨어 인터페이스로 이전
- [ ] C# GUI 연동 (rosbridge 웹소켓 또는 UDP)
